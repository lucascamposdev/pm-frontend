const manager = {email: 'manager@test.com', password: '123456'}
const collaborator = {email: 'colaborator@test.com', password: '123456'}

const credentials = {
    manager,
    collaborator
}

export default credentials;